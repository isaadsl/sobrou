export declare const getCSS: ({ uid, themeColor, height, width, allowFullscreen, }: {
    uid: string;
    themeColor: string;
    height: string;
    width: string;
    allowFullscreen: boolean;
}) => string;
